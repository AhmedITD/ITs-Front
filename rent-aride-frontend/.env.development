# Same-origin prefix so Vite proxies to backend (no CORS)
VITE_API_URL=http://localhost:5022

# Backend URL for the proxy (update when tunnel URL changes)
VITE_PROXY_TARGET=https://kzcajtqhoz.a.pinggy.link
